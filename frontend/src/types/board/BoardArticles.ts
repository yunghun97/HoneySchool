interface BoardArticles {
    id: number,
    category: string,
    title: string,
    content?: string,
    writer: string,
    date: number,
  }
// type boardType = Array<BoardArticles>;
export default BoardArticles